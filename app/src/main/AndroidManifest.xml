<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.responsi135">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mb2"></activity>
        <activity android:name=".spiker2" />
        <activity android:name=".cd2" />
        <activity android:name=".ssd2" />
        <activity android:name=".ram2" />
        <activity android:name=".monitor2" />
        <activity android:name=".cpu2" />
        <activity android:name=".keyboard2" />
        <activity android:name=".mose" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>